{{- range $name, $svc := .Values.services }}
{{- $imageName := default (printf "%s-service" $name) $svc.image }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $name }}-service
  namespace: {{ $.Values.global.namespace }}
  labels:
    app: {{ $name }}-service
    version: {{ $.Chart.AppVersion }}
    pillar: kinara
spec:
  replicas: {{ $svc.replicas }}
  selector:
    matchLabels:
      app: {{ $name }}-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: {{ $name }}-service
        version: {{ $.Chart.AppVersion }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ $svc.port | quote }}
        prometheus.io/path: "/metrics"
    spec:
      {{- with $.Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsUser: 1000
        runAsNonRoot: true
        fsGroup: 1000
      containers:
        - name: {{ $name }}-service
          image: {{ $.Values.global.imageRegistry }}/{{ $imageName }}:{{ $.Values.global.imageTag | default "latest" }}
          imagePullPolicy: {{ $.Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ $svc.port }}
              name: http
          env:
            - name: PORT
              value: {{ $svc.port | quote }}
            # DB credentials from shared secret; DATABASE_URL assembled via K8s env var interpolation
            - name: _DB_HOST
              valueFrom:
                secretKeyRef:
                  name: kinara-db-credentials
                  key: host
            - name: _DB_PORT
              valueFrom:
                secretKeyRef:
                  name: kinara-db-credentials
                  key: port
            - name: _DB_USER
              valueFrom:
                secretKeyRef:
                  name: kinara-db-credentials
                  key: user
            - name: _DB_PASS
              valueFrom:
                secretKeyRef:
                  name: kinara-db-credentials
                  key: pass
            - name: DATABASE_URL
              value: "postgresql://$(_DB_USER):$(_DB_PASS)@$(_DB_HOST):$(_DB_PORT)/{{ $svc.db }}?sslmode=require"
            - name: JWT_PUBLIC_KEY_PATH
              value: {{ $.Values.global.jwtPublicKeyPath }}
            - name: REDIS_URL
              value: "redis://kinara-redis:6379"
            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: kinara-shared-secrets
                  key: encryption-key
          resources:
            {{- if $.Values.defaultResources }}
            {{- toYaml $.Values.defaultResources | nindent 12 }}
            {{- else }}
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
            {{- end }}
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          livenessProbe:
            httpGet:
              path: /health
              port: {{ $svc.port }}
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ $svc.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          volumeMounts:
            - name: jwt-public-key
              mountPath: /etc/jwt
              readOnly: true
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: jwt-public-key
          secret:
            secretName: kinara-jwt-public-key
        - name: tmp
          emptyDir: {}
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $name }}-service
  namespace: {{ $.Values.global.namespace }}
  labels:
    app: {{ $name }}-service
spec:
  type: ClusterIP
  selector:
    app: {{ $name }}-service
  ports:
    - port: {{ $svc.port }}
      targetPort: {{ $svc.port }}
      name: http
{{- if $.Values.hpa.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ $name }}-service-hpa
  namespace: {{ $.Values.global.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ $name }}-service
  minReplicas: {{ $svc.replicas }}
  maxReplicas: {{ $svc.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
{{- end }}
{{- end }}
