apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kinara-tls-production
  namespace: kinara-production
spec:
  secretName: kinara-tls-production
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: api.kinaraos.com
  dnsNames:
    - api.kinaraos.com
    - admin.kinaraos.com
