---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: referral-service
  namespace: kinara-staging
  labels:
    app: referral-service
    version: "1.0.0"
    part-of: kinara-os
spec:
  replicas: 2
  selector:
    matchLabels:
      app: referral-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: referral-service
        version: "1.0.0"
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: referral-service-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: referral-service
          image: registry.digitalocean.com/kinara-os/referral-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: https
              protocol: TCP
          env:
            - name: PORT
              value: "8080"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: referral-service-secrets
                  key: database-url
            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: referral-service-secrets
                  key: encryption-key
            - name: REDIS_ADDR
              valueFrom:
                secretKeyRef:
                  name: referral-service-secrets
                  key: redis-addr
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: referral-service-secrets
                  key: redis-password
            - name: JWT_PUBLIC_KEY_PATH
              value: /app/certs/jwt-public.pem
            - name: TLS_CERT_PATH
              value: /app/certs/tls.crt
            - name: TLS_KEY_PATH
              value: /app/certs/tls.key
            - name: CA_CERT_PATH
              value: /app/certs/ca.crt
          volumeMounts:
            - name: tls-certs
              mountPath: /app/certs
              readOnly: true
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
              scheme: HTTPS
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
              scheme: HTTPS
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      volumes:
        - name: tls-certs
          secret:
            secretName: referral-service-tls
      imagePullSecrets:
        - name: do-registry-secret

---
apiVersion: v1
kind: Service
metadata:
  name: referral-service
  namespace: kinara-staging
  labels:
    app: referral-service
spec:
  selector:
    app: referral-service
  ports:
    - name: https
      port: 443
      targetPort: 8080
      protocol: TCP
  type: ClusterIP

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: referral-service-sa
  namespace: kinara-staging
  labels:
    app: referral-service

---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: referral-service-pdb
  namespace: kinara-staging
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: referral-service

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: referral-service-hpa
  namespace: kinara-staging
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: referral-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
