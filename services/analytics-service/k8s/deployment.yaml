apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: kinara-os
spec:
  replicas: 2
  selector:
    matchLabels:
      app: analytics-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: analytics-service
    spec:
      securityContext:
        runAsUser: 1000
        runAsNonRoot: true
      containers:
      - name: analytics-service
        image: registry.digitalocean.com/kinara/analytics-service:latest
        ports:
        - containerPort: 8108
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: analytics-service-secrets
              key: database-url
        - name: JWT_PUBLIC_KEY_PATH
          value: /etc/jwt/public.pem
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: shared-secrets
              key: redis-url
        - name: AES_KEY
          valueFrom:
            secretKeyRef:
              name: shared-secrets
              key: aes-key
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "2Gi"
        securityContext:
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
        livenessProbe:
          httpGet:
            path: /health
            port: 8108
          initialDelaySeconds: 10
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 8108
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: kinara-os
spec:
  selector:
    app: analytics-service
  ports:
  - port: 8108
    targetPort: 8108
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: analytics-service-hpa
  namespace: kinara-os
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: analytics-service
  minReplicas: 2
  maxReplicas: 20
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: analytics-service-pdb
  namespace: kinara-os
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: analytics-service
